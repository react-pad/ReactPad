[build]
command = "npm run build"
publish = ".next"

[[plugins]]
package = "@netlify/plugin-nextjs"

# This redirects all requests to the Next.js handler,
# which is necessary for dynamic routes and server-side rendering.
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
